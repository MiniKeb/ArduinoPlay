#include <iostream>
#include <assert.h>
#include "Ribbon.h"

int main(int argc, char const *argv[])
{
    std::cout << "I test nothing." << std::endl;
	//throw std::exception("Pas bon");
	assert(false);
	return 0;
}
